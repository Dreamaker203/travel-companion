# 旅行伴侣 · 软件需求规格说明书

> **Travel Companion · Software Requirements Specification**
>
> 一款为重度旅行者设计的、全生命周期的个人旅行操作系统

| 项目 | 内容 |
|---|---|
| 版本 | v0.1 (MVP 设计版) |
| 作者 | Dreamaker (刘忠鑫) |
| 编写日期 | 2026.04.21 |
| 文档状态 | 设计阶段 · 待开发 |
| 项目仓库 | 待创建 |

---

## 目录

- [1. 项目背景与愿景](#1-项目背景与愿景)
- [2. 用户画像](#2-用户画像)
- [3. 竞品分析与差异化定位](#3-竞品分析与差异化定位)
- [4. 产品定位与核心价值](#4-产品定位与核心价值)
- [5. 功能需求](#5-功能需求)
- [6. 数据模型设计](#6-数据模型设计)
- [7. 技术架构与选型](#7-技术架构与选型)
- [8. 接口设计](#8-接口设计)
- [9. 非功能需求](#9-非功能需求)
- [10. 版本规划与里程碑](#10-版本规划与里程碑)
- [11. 风险与假设](#11-风险与假设)
- [附录](#附录)

---

## 1. 项目背景与愿景

### 1.1 问题起源

作者是一名经常带队出行的旅行者，长期作为"组织者"为朋友规划行程、安排路线、核算账单。在长期使用中发现，现有的旅行 app 都无法完整覆盖"组织者"这一角色的全部需求：

- **OTA 类 app**（携程、飞猪）侧重交易与售卖，规划工具是附属功能，信息过载且商业化严重。
- **内容社区类 app**（马蜂窝、小红书）擅长"去哪玩"的决策，但不提供结构化规划能力。
- **纯规划工具类 app**（圆周旅迹、路敢敢）产品哲学简洁，但聚焦单次出行，缺乏多人协作深度、账单分摊、长期留存等重度用户刚需。
- **地图类 app**（高德）是行中高频工具，但不懂"多日行程"的概念。

**核心观察**：用户为完成一次完整的旅行，需要在 4-5 个 app 之间反复切换——小红书看攻略、圆周旅迹排行程、携程订酒店、高德找路、微信群发截图、Splitwise 分账。每个 app 都只解决了一个片段，没有一个 app 把"**决策 → 规划 → 执行 → 结算 → 留存**"串成完整闭环。

### 1.2 项目愿景

构建一款**重度旅行者的全生命周期伴侣工具**，覆盖旅行的五个阶段：

1. **规划前**（Pre-Plan）—— 灵感收集、候选池管理
2. **规划中**（Planning）—— 行程排期、路线优化、预算设定
3. **旅行中**（Traveling）—— 凭证查阅、记账、导航跳转
4. **旅行后**（Settling）—— 账单分摊、回忆录生成
5. **长期沉淀**（Archive）—— 足迹地图、年度报告、经验复用

### 1.3 项目目标

| 目标类型 | 具体内容 |
|---|---|
| 用户目标 | 让一次多人旅行的组织负担从"规划 + 沟通 + 结算 + 分享"四份工作降到一份 |
| 产品目标 | 成为作者及其朋友圈子里出行规划的默认工具 |
| 学习目标 | 完整走通"需求分析 → 产品设计 → 技术选型 → 全栈实现"闭环，作为秋招与 2027 考研复试的代表作 |
| 技术目标 | 实践 Flutter + FastAPI 跨端全栈开发、本地优先架构、LLM API 应用 |

---

## 2. 用户画像

### 2.1 目标用户

| 画像维度 | 描述 |
|---|---|
| **角色** | 朋友圈中的"旅行组织者"——在多人出行中主动承担规划工作的那个人 |
| **年龄** | 22-35 岁 |
| **旅行频次** | 每年 4+ 次，含周末短途、长假深度游、出差商务、出境游 |
| **技术水平** | 熟悉智能手机，愿意尝试新工具，对隐私和数据导出敏感 |
| **核心诉求** | 规划高效、队友友好、账单清晰、回忆长存 |
| **典型场景** | 和 3-4 个朋友一起出行 5-8 天，自己负责订酒店和票、安排每日行程、AA 分账 |

### 2.2 反向画像（不是目标用户）

为了聚焦 MVP，以下类型**不作为**主要优化对象：

- 跟团游用户（行程由旅行社给定）
- 一年旅行一次以下的低频用户（用备忘录足矣，无需专门工具）
- 纯商务差旅用户（公司差旅系统更合适）
- 纯独自旅行用户（很多"队友协作"功能对他们冗余，但不妨碍单人使用）

### 2.3 用户核心场景

**场景 A — 节前一周紧急规划**。用户周四晚下班，下周一要带 4 个朋友去北京 5 天。需要在周末 2 天内：查攻略、定酒店、排 5 天行程、发给队友群里让大家看一眼提意见。

**场景 B — 旅行中随手记账**。用户在簋街吃完小龙虾，当场支付了 720 元。需要快速记一笔"我垫付、4 人 AA、餐饮类"，10 秒内完成，不影响聊天。

**场景 C — 旅行结束后的结算与分享**。回到家的第二天晚上，需要生成一份清晰的账单让大家转账，同时想留一份"回忆册"作为纪念，也想发朋友圈装一下。

**场景 D — 半年后想再去**。用户计划春节再次去大理。打开 app 时，希望看到"上次你去过哪些地方、哪些没去"，避免重复走已经去过的景点。

---

## 3. 竞品分析与差异化定位

### 3.1 主要竞品

基于调研，国内外旅行 app 分为四个阵营，对每个阵营选取代表产品进行分析：

| 阵营 | 代表产品 | 核心定位 |
|---|---|---|
| 纯规划工具 | 圆周旅迹、路敢敢（原识途）、Wanderlog、TripIt | 极简行程排期工具 |
| OTA 交易 | 携程、飞猪、去哪儿 | 机票酒店门票交易 |
| 内容社区 | 马蜂窝、小红书 | 攻略种草与分享 |
| 地图导航 | 高德、Google Maps | 路线与周边查询 |

### 3.2 核心竞品深度对比

| 功能维度 | 圆周旅迹 | 路敢敢 | 携程 | 小红书 | **本项目** |
|---|:-:|:-:|:-:|:-:|:-:|
| 小红书/公众号一键导入 | 强 | 强 | 无 | —— | 弱（不做） |
| 地图 + 智能路线优化 | 强 | 强 | 一般 | 无 | 一般（调 API） |
| 好友协作编辑 | 一般 | 一般 | 无 | 无 | **强** |
| PDF / 长图导出 | 一般 | 一般 | 无 | 无 | **强** |
| 多币种记账 + AA 分摊 | 弱 | 弱 | 无 | 无 | **强** |
| 凭证 / 订单集中管理 | 弱 | 弱 | 半自动 | 无 | **强** |
| 打包清单 + 模板 | 一般 | 弱 | 无 | 无 | **强** |
| AI 引言 / 游记生成 | 弱 | 弱 | 无 | 无 | **强（v2）** |
| 终身足迹地图 | 弱 | 弱 | 无 | 无 | **强（v2）** |
| 纪念册 / 年度报告 | 弱 | 弱 | 无 | 无 | **强（v2）** |
| 机酒交易 | 无 | 无 | 极强 | 无 | 无（跳转） |
| UGC 攻略 | 一般 | 一般 | 一般 | 极强 | 无（不做） |

### 3.3 竞品洞察总结

**洞察 1：极简规划器赛道已被圆周旅迹占住**。该赛道的用户审美已被定义（简洁、扁平、免费、无广告），拼"一键抄作业"、社区攻略库的丰富度不是个人项目的优势。

**洞察 2：现有工具都忽略"组织者 vs 同行者"的角色区分**。它们把"多人旅行"降级为"多人编辑同一份行程"，但组织者需要的是更丰富的视角——队友友好的导出、AA 账单的透明结算、队友问题的集中答疑。

**洞察 3：几乎所有产品都在旅行结束后沉默**。行程数据、花销记录、轨迹轨迹这些结构化数据是天然的回忆素材，却没有产品把它们串成"可以反复回看的资产"。

**洞察 4：市场上缺乏一款"全生命周期"工具**。规划、协作、结算、留存分别被 4 类工具分割，用户在它们之间来回切换，信息反复抄录。一个打通闭环的工具有真实的空白机会。

### 3.4 差异化定位

本项目的差异化定位用一句话概括：

> **为经常带队出行的重度规划者设计的、反商业化的、覆盖旅行全生命周期的个人操作系统。**

三个"别人不做的我来做"：

1. **别人做快速规划，我做完整规划**——深度优先，目标是资深用户而非小白
2. **别人做行程前中，我做全生命周期**——加入留存、足迹、年度报告
3. **别人做个人工具+协作，我做组织者工具+队友友好**——明确的角色区分和 AA 分摊

---

## 4. 产品定位与核心价值

### 4.1 一句话定位

**个人旅行操作系统（Personal Travel OS）** —— 规划、执行、结算、留存一体。

### 4.2 核心价值主张

| 面向 | 价值 |
|---|---|
| 组织者（用户本人） | 把 4 个 app 的工作合并到 1 个，减少信息搬运 |
| 同行者（用户的朋友） | 一眼看懂行程、费用透明、不用追问组织者 |
| 长期使用者 | 每一次旅行都沉淀成可回看的资产，不被遗忘 |

### 4.3 产品原则

1. **本地优先（Local-First）**——所有数据首先写入本地，云端只是备份和同步，保证任何网络环境可用
2. **反商业化（Ad-Free）**——无广告、无推荐位、无诱导付费，工具属性纯粹
3. **结构化即价值**——行程、花销、照片、凭证都是结构化数据，这是相对于"备忘录+朋友圈"的核心优势
4. **沉默优于打扰**——仅在关键时刻（出发前一天、旅行结束、年度）主动推送，其他时间保持安静

---

## 5. 功能需求

### 5.1 功能总览

按 MVP 优先级分为三个版本：

**MVP（v1.0）** —— 规划与导出核心
**v1.5** —— 协作与账单
**v2.0** —— 留存与纪念

### 5.2 MVP 功能清单（v1.0）

#### 5.2.1 模块 A：旅行管理

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---|---|:-:|---|
| F-A-01 | 新建旅行 | P0 | 支持填写标题、日期区间、多目的地、预算、币种、封面 |
| F-A-02 | 旅行列表 | P0 | 按状态筛选（规划中/进行中/已结束），卡片式展示 |
| F-A-03 | 旅行详情总览 | P0 | 基本信息 + 每日概览 + 预算进度 |
| F-A-04 | 快速模板 | P1 | 预设"周末短途/徒步登山/出差商务/海岛度假"等模板，一键生成默认结构 |
| F-A-05 | 归档与删除 | P0 | 长按旅行卡可归档（保留数据但隐藏）或永久删除 |

#### 5.2.2 模块 B：行程规划（核心）

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---|---|:-:|---|
| F-B-01 | 按天排期 | P0 | 旅行按天拆分为 D1、D2、…Dn，每天独立编辑 |
| F-B-02 | 活动增删改 | P0 | 活动包含类型（景点/餐饮/交通/住宿/自由）、时间、地点、费用、备注、图片 |
| F-B-03 | 时间轴视图 | P0 | 一天活动按时间先后垂直展示，类型用颜色区分 |
| F-B-04 | 拖拽排序 | P0 | 长按活动卡可拖动，支持跨天移动 |
| F-B-05 | 待定池 | P1 | 收集尚未确定日期的候选活动，支持拖入具体某天 |
| F-B-06 | 地点选择 | P0 | 调用高德 POI 搜索，返回经纬度和地址 |
| F-B-07 | 多币种支持 | P1 | 活动费用可选币种，自动按当前汇率换算汇总 |

#### 5.2.3 模块 C：路线与地图

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---|---|:-:|---|
| F-C-01 | 每日路线展示 | P0 | 在地图上用数字标注站点并连线 |
| F-C-02 | 多种交通方式 | P1 | 步行/驾车/公交切换，显示每段距离与耗时 |
| F-C-03 | 智能路线优化 | P2 | 按地理位置重排当天活动，减少走回头路 |
| F-C-04 | 一键导航 | P1 | 点击活动 → 跳转高德 app 导航 |

#### 5.2.4 模块 D：预算与费用

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---|---|:-:|---|
| F-D-01 | 预算设定 | P0 | 总预算 + 五大分类（景点/餐饮/交通/住宿/购物） |
| F-D-02 | 实时进度 | P0 | 卡片式展示已用/总预算/剩余天数/超支预警 |
| F-D-03 | 记一笔 | P0 | 快速录入一笔支出：金额、类别、时间、备注、付款人 |
| F-D-04 | 分类明细 | P0 | 按类别展开、每笔支出可编辑 |
| F-D-05 | 超支预警 | P1 | 某类别超支时在进度条上标红 |

#### 5.2.5 模块 E：导出与分享

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---|---|:-:|---|
| F-E-01 | PDF 详细版 | P0 | A4 规格，完整行程，含当日须知、费用、凭证图 |
| F-E-02 | PNG 精简版 | P0 | 正方形卡片，一天一张，适合朋友圈分享 |
| F-E-03 | 账单版 PDF | P0 | 垫付明细、最简转账方案、分类汇总 |
| F-E-04 | 地图路线 PNG | P1 | 在地图底图上画路线与站点 |
| F-E-05 | 模板预览 | P0 | 导出前可实时预览效果 |
| F-E-06 | 一键分享 | P0 | 调用系统分享面板，支持微信、微信群、朋友圈、链接 |

### 5.3 v1.5 功能清单（协作与账单）

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---|---|:-:|---|
| F-F-01 | 同行者管理 | P0 | 每次旅行添加同行者（姓名、头像色），区分"组织者"与"同行者" |
| F-F-02 | 只读分享链接 | P0 | 生成链接让同行者在线查看行程（无需注册） |
| F-F-03 | 同行者留言 | P1 | 同行者可对特定活动留言（"这里想加 X"） |
| F-F-04 | AA 分账 | P0 | 每笔支出可选分摊方式（AA/按比例/单独承担） |
| F-F-05 | 最简转账方案 | P0 | 自动计算最少转账次数，生成"谁转多少给谁"图示 |
| F-F-06 | 凭证管理 | P1 | 护照、机票、酒店订单等集中存放，离线可查 |
| F-F-07 | 打包清单 | P1 | 按旅行类型预设模板（徒步/商务/海岛），一键生成检查清单 |

### 5.4 v2.0 功能清单（留存与纪念）

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---|---|:-:|---|
| F-G-01 | 行中自动沉淀 | P0 | 照片按时间+GPS 自动关联到行程；记账数据自动沉淀；轨迹后台记录 |
| F-G-02 | AI 引言生成 | P0 | 调用 LLM，输入每日行程数据，输出 30 字左右文艺化引言作为草稿 |
| F-G-03 | 旅行回忆录 | P0 | 每次旅行自动生成"电子书"，每天一页，含照片、引言、路线、花销 |
| F-G-04 | 纪念册导出 | P1 | 回忆录可导出为 PDF / 长图 / 电子书格式，可对接实体印刷服务 |
| F-G-05 | 终身足迹地图 | P0 | 历史所有旅行聚合到一张地图，点亮省份和国家 |
| F-G-06 | 足迹数据统计 | P0 | 总国家数、城市数、总里程、最常去 |
| F-G-07 | 年度报告 | P1 | 类似网易云音乐年度总结，带分享卡片 |
| F-G-08 | 时光回访提醒 | P2 | "三年前的今天你在 XX"，定时推送 |
| F-G-09 | 同行人关联 | P2 | "你和 XX 一起去过 N 次"视图 |
| F-G-10 | 历史经验复用 | P1 | 再次规划同一城市时提示"上次去过的地方" |

### 5.5 不做的功能（明确排除）

为避免 scope creep，以下功能明确**不在任何版本**中实现：

- 机票/酒店交易（跳转携程、飞猪即可）
- UGC 社区攻略浏览（小红书已满足）
- 自研地图（调用高德 API 即可）
- 翻译、汇率查询等辅助工具（有更专业的 app）
- 抽奖、盲盒、营销活动等商业化玩法

---

## 6. 数据模型设计

### 6.1 核心实体关系图

```
User (用户)
  │
  ├── owns ──> Trip (旅行)
  │              │
  │              ├── has ──> Day (每日)
  │              │             │
  │              │             └── contains ──> Activity (活动)
  │              │                                  │
  │              │                                  └── has ──> Expense (支出)
  │              │
  │              ├── has ──> CandidatePool (待定池)
  │              ├── has ──> Companion[] (同行者)
  │              ├── has ──> Voucher[] (凭证)
  │              ├── has ──> PackList (打包清单)
  │              └── generates ──> MemoryBook (回忆录) [v2]
  │
  └── owns ──> FootprintMap (终身足迹) [v2]
```

### 6.2 主要表结构（PostgreSQL + SQLite 双端）

#### users

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| phone | VARCHAR(20) | 登录手机号 |
| nickname | VARCHAR(50) | 昵称 |
| avatar_url | TEXT | 头像 URL |
| preferences | JSONB | 用户偏好（主题色、默认币种等） |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

#### trips

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| user_id | UUID (FK) | 所属用户 |
| title | VARCHAR(100) | 旅行标题 |
| cover_url | TEXT | 封面图 URL |
| destinations | JSONB | 目的地数组 `["北京", "天津"]` |
| start_date | DATE | 开始日期 |
| end_date | DATE | 结束日期 |
| total_budget | DECIMAL(10,2) | 总预算 |
| currency | VARCHAR(3) | 币种代码（ISO 4217） |
| status | ENUM | planning / ongoing / completed / archived |
| template_type | VARCHAR(50) | 使用的模板类型（可空） |
| version | INTEGER | 乐观锁版本号 |
| is_deleted | BOOLEAN | 软删除标记 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

**索引**：`(user_id, status)`、`(user_id, start_date)`、`(user_id, is_deleted)`

#### days

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| trip_id | UUID (FK) | 所属旅行 |
| day_number | INTEGER | 第几天（1-indexed） |
| date | DATE | 具体日期 |
| note | TEXT | 当日备注 |
| weather | JSONB | 当日天气缓存（可空） |
| version | INTEGER | 乐观锁版本号 |

**索引**：`(trip_id, day_number)` 唯一

#### activities

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| trip_id | UUID (FK) | 冗余字段，便于查询 |
| day_id | UUID (FK, NULL) | 所属日；为空表示在待定池 |
| type | ENUM | scenic / dining / transport / lodging / free |
| title | VARCHAR(100) | 名称 |
| location_name | VARCHAR(200) | 地点名称 |
| location_address | TEXT | 详细地址 |
| longitude | DECIMAL(10,7) | 经度 |
| latitude | DECIMAL(10,7) | 纬度 |
| start_time | TIME | 开始时间 |
| duration_minutes | INTEGER | 预估时长（分钟） |
| estimated_cost | DECIMAL(10,2) | 预估费用 |
| actual_cost | DECIMAL(10,2) | 实际费用（可空） |
| currency | VARCHAR(3) | 币种 |
| note | TEXT | 备注 |
| images | JSONB | 图片 URL 数组 |
| order_index | INTEGER | 当日排序 |
| is_booked | BOOLEAN | 是否已预订 |
| booking_info | JSONB | 预订信息（订单号、平台） |
| version | INTEGER | 乐观锁 |
| is_deleted | BOOLEAN | 软删除 |

**索引**：`(trip_id, day_id, order_index)`、`(trip_id, day_id IS NULL)`

#### expenses

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| trip_id | UUID (FK) | 所属旅行 |
| activity_id | UUID (FK, NULL) | 关联活动（可空） |
| amount | DECIMAL(10,2) | 金额 |
| currency | VARCHAR(3) | 币种 |
| category | ENUM | scenic / dining / transport / lodging / shopping / other |
| paid_by | UUID | 付款人（同行者 ID 或用户 ID） |
| split_method | ENUM | aa / ratio / single |
| split_detail | JSONB | 分摊明细（按比例时） |
| occurred_at | TIMESTAMP | 发生时间 |
| note | TEXT | 备注 |

#### companions

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| trip_id | UUID (FK) | 所属旅行 |
| name | VARCHAR(50) | 姓名 |
| avatar_color | VARCHAR(7) | 头像色（hex） |
| role | ENUM | organizer / member |
| contact | VARCHAR(100) | 联系方式（可空） |
| created_at | TIMESTAMP | 创建时间 |

#### vouchers

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| trip_id | UUID (FK) | 所属旅行 |
| type | ENUM | passport / visa / ticket / hotel / insurance / other |
| title | VARCHAR(100) | 凭证名称 |
| local_file_path | TEXT | 本地文件路径（本地存储优先） |
| cloud_url | TEXT | 云备份 URL（可空） |
| metadata | JSONB | 订单号、有效期等 |
| created_at | TIMESTAMP | 创建时间 |

#### memory_books（v2）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID (PK) | 主键 |
| trip_id | UUID (FK) | 所属旅行 |
| generated_content | JSONB | 每日引言、照片关联、统计数据 |
| template | VARCHAR(20) | 模板类型 |
| last_generated_at | TIMESTAMP | 最近生成时间 |

### 6.3 同步字段设计

所有需要云端同步的表增加以下字段，支持断网操作和冲突解决：

| 字段 | 类型 | 说明 |
|---|---|---|
| version | INTEGER | 乐观锁，每次更新 +1 |
| is_deleted | BOOLEAN | 软删除标记 |
| last_synced_at | TIMESTAMP | 最近同步时间 |
| sync_status | ENUM | synced / pending / conflict |

**同步策略**：Last-Write-Wins（后写覆盖），冲突时保留两版本供用户手动选择。

---

## 7. 技术架构与选型

### 7.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│  Flutter 前端 (iOS / Android)                        │
│  ├─ Pages: Trip List / Trip Detail / Map / Export   │
│  ├─ State: Riverpod                                  │
│  └─ PDF/PNG: pdf + screenshot 包                     │
└──────────────────┬──────────────────────────────────┘
                   │ HTTPS + JWT
┌──────────────────▼──────────────────────────────────┐
│  本地层                                              │
│  ├─ SQLite (sqflite)                                 │
│  ├─ 文件缓存（照片、凭证、地图瓦片）                 │
│  └─ 同步引擎（后台异步）                             │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│  FastAPI 后端                                        │
│  ├─ Auth API  (JWT)                                  │
│  ├─ Trip API  (CRUD + 同步)                          │
│  ├─ AI API    (LLM 引言生成) [v2]                    │
│  └─ Third-party Adapters (高德/天气/汇率)            │
└──────┬────────────────────────────────┬─────────────┘
       │                                │
┌──────▼──────┐                  ┌──────▼──────┐
│ PostgreSQL  │                  │    Redis    │
│ (持久化)    │                  │ (缓存/会话) │
└─────────────┘                  └─────────────┘
```

### 7.2 技术选型

| 层级 | 选型 | 理由 |
|---|---|---|
| 前端框架 | Flutter 3.x | 跨端一致性、PDF/地图生态成熟、对后端工程师友好 |
| 状态管理 | Riverpod 2.x | 响应式、可测试性好 |
| 本地数据库 | sqflite | Flutter 生态标准方案 |
| PDF 生成 | pdf + printing 包 | 纯 Dart、客户端生成、速度快 |
| 后端框架 | FastAPI | 作者熟悉、Python 生态、自动 OpenAPI 文档 |
| 数据库 | PostgreSQL 16 | 成熟可靠、JSONB 支持、全文搜索好 |
| 缓存 | Redis 7 | 会话、验证码、限流 |
| 地图 | 高德 API | 国内最成熟、境内外覆盖 |
| LLM API | Qwen / Moonshot | 国内合规、价格低、中文能力好 |
| 对象存储 | 阿里云 OSS | 照片、凭证、封面图 |
| 部署 | Docker Compose | 单机部署，够用 |

### 7.3 关键架构决策

**决策 1：本地优先（Local-First）**
- 所有写操作先落地本地 SQLite
- 后台异步推送云端
- 网络不可用时完全可用，仅同步延迟

**决策 2：PDF 客户端生成**
- 使用 Flutter pdf 包直接在端上生成
- 不依赖服务端，响应快、省服务器成本、隐私友好

**决策 3：第三方服务跳转而非集成**
- 订票 → 跳转携程/飞猪深链接
- 导航 → 跳转高德/百度 app
- 降低复杂度、规避资质问题、专注核心价值

**决策 4：LLM 仅用于 v2 纪念模块**
- MVP 不依赖 AI，避免增加不确定性
- v2 调用 LLM 生成引言、年度总结文案等轻量场景
- 控制 API 成本，单用户每年生成额度有限

---

## 8. 接口设计

### 8.1 接口规范

- Base URL: `https://api.travel-companion.example.com/v1`
- 认证：所有需授权接口在 Header 携带 `Authorization: Bearer <JWT>`
- 响应格式：统一 JSON，结构如下：

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

- 错误码约定：
  - `0`：成功
  - `40xxx`：客户端错误（参数、权限）
  - `50xxx`：服务端错误

### 8.2 核心接口清单

#### 认证模块

| Method | Path | 说明 |
|---|---|---|
| POST | `/auth/send-code` | 发送短信验证码 |
| POST | `/auth/login` | 验证码登录 |
| POST | `/auth/refresh` | 刷新 token |
| POST | `/auth/logout` | 登出 |

#### 旅行模块

| Method | Path | 说明 |
|---|---|---|
| GET | `/trips` | 获取旅行列表（支持状态筛选） |
| POST | `/trips` | 新建旅行 |
| GET | `/trips/{id}` | 获取旅行详情 |
| PUT | `/trips/{id}` | 更新旅行 |
| DELETE | `/trips/{id}` | 软删除 |
| POST | `/trips/{id}/archive` | 归档 |

#### 行程与活动

| Method | Path | 说明 |
|---|---|---|
| GET | `/trips/{id}/days` | 获取所有天 |
| PUT | `/trips/{id}/days/{day_id}` | 更新单日信息 |
| GET | `/trips/{id}/activities` | 获取活动列表（含待定池） |
| POST | `/trips/{id}/activities` | 新建活动 |
| PUT | `/activities/{id}` | 更新活动 |
| DELETE | `/activities/{id}` | 删除活动 |
| POST | `/activities/{id}/move` | 移动活动到另一天 |
| POST | `/trips/{id}/optimize-route` | 智能路线优化 |

#### 预算与费用

| Method | Path | 说明 |
|---|---|---|
| GET | `/trips/{id}/expenses` | 获取支出列表 |
| POST | `/trips/{id}/expenses` | 记一笔 |
| PUT | `/expenses/{id}` | 更新 |
| DELETE | `/expenses/{id}` | 删除 |
| GET | `/trips/{id}/settlement` | 计算最简转账方案 |

#### 同步（关键）

| Method | Path | 说明 |
|---|---|---|
| GET | `/sync/pull?since={timestamp}` | 拉取增量变更 |
| POST | `/sync/push` | 推送本地变更 |

**同步请求体示例**：

```json
{
  "trips": [
    {
      "id": "uuid",
      "version": 5,
      "operation": "update",
      "data": { ... }
    }
  ],
  "activities": [...],
  "expenses": [...]
}
```

**冲突响应**：

```json
{
  "code": 0,
  "data": {
    "applied": ["activity-uuid-1"],
    "conflicts": [
      {
        "id": "activity-uuid-2",
        "local_version": 3,
        "server_version": 4,
        "server_data": { ... }
      }
    ]
  }
}
```

#### 导出

| Method | Path | 说明 |
|---|---|---|
| GET | `/trips/{id}/export/pdf?template=detailed` | 生成详细版 PDF（可选服务端生成） |
| GET | `/trips/{id}/export/pdf?template=settlement` | 生成账单版 PDF |

#### v2 · AI 与纪念

| Method | Path | 说明 |
|---|---|---|
| POST | `/ai/generate-caption` | 为单日生成 AI 引言 |
| POST | `/trips/{id}/memory-book/generate` | 生成回忆录 |
| GET | `/users/me/footprint` | 获取终身足迹数据 |
| GET | `/users/me/annual-report/{year}` | 获取年度报告 |

### 8.3 关键接口详解

#### POST `/ai/generate-caption`（v2）

**请求体**：

```json
{
  "trip_id": "uuid",
  "day_id": "uuid",
  "context": {
    "date": "2026-05-04",
    "activities": ["故宫博物院", "景山公园", "四季民福", "南锣鼓巷"],
    "weather": "晴 22°C",
    "total_cost": 410,
    "photo_count": 38,
    "mood_keywords": []
  }
}
```

**响应**：

```json
{
  "code": 0,
  "data": {
    "caption": "故宫的午后出奇地安静，阳光打在金色的琉璃瓦上，像是历史在发烫。",
    "alternatives": [
      "一天走过六百年，脚步比记忆更诚实。",
      "从午门到神武门，把一本历史书翻得有些轻。"
    ]
  }
}
```

**Prompt 设计思路**（文档化，便于后续优化）：

```
System: 你是一位文笔细腻的旅行散文作家，帮助用户为一天的旅行生成 30 字左右的开篇引言。
风格要求：文艺但不矫情，具象而非概括，用当天的具体细节而非通用词汇。

User: [上下文]
Output: 只输出引言本身，不要解释。给出 3 个风格各异的备选。
```

---

## 9. 非功能需求

### 9.1 性能要求

| 场景 | 指标 |
|---|---|
| App 冷启动到首屏 | < 2s |
| 行程编辑页交互响应 | < 100ms |
| PDF 生成（单次旅行） | < 3s |
| 地图加载首屏 | < 1.5s |
| 同步单次推送 | < 500ms |
| LLM 引言生成 | < 5s |

### 9.2 可用性要求

- 核心功能（查看/编辑已有行程、记账、查看凭证）**离线完全可用**
- 网络恢复后自动同步，用户无感
- 单机单用户数据量上限：500 次旅行、10000 个活动、100000 笔支出

### 9.3 安全要求

- 用户数据加密传输（HTTPS）
- 敏感信息（凭证图片、护照号）本地加密存储
- 分享链接有过期时间，可随时撤销
- 云端数据定期备份，保留 30 天历史版本

### 9.4 隐私要求

- 位置数据、照片仅用于行程关联，不用于推荐算法
- 不收集好友关系、社交链
- 支持完整数据导出（JSON 格式）
- 支持账号注销与数据删除

### 9.5 兼容性要求

- iOS 14.0+
- Android 8.0+ (API 26)
- 屏幕适配：iPhone SE 到 iPhone Pro Max、小米到折叠屏

### 9.6 可维护性要求

- 后端代码测试覆盖率 > 70%
- 关键业务路径（创建旅行、同步、导出）有端到端测试
- API 向后兼容，至少保留两个版本
- 日志分级清晰，错误可追溯

---

## 10. 版本规划与里程碑

### 10.1 版本规划

| 版本 | 目标 | 预计时间 | 状态 |
|---|---|---|---|
| v0.1 | 设计文档 + UI 原型 | 2026.04 | 进行中 |
| v1.0 MVP | 规划 + 地图 + 导出（单人可用） | 2026.05 – 2026.08 | 待开始 |
| v1.5 | 协作 + 账单 + 凭证 | 2026.09 – 2026.12 | 规划中 |
| v2.0 | AI 引言 + 回忆录 + 足迹 + 年度报告 | 2027.01 – 2027.06 | 规划中 |

### 10.2 MVP 开发里程碑（v1.0）

| 阶段 | 周数 | 任务 |
|---|---|---|
| 1. 环境与骨架 | 第 1 周 | Flutter 项目初始化、FastAPI 后端骨架、数据库建表、CI/CD |
| 2. 认证 + 用户 | 第 2 周 | 短信登录、JWT、用户偏好 |
| 3. 旅行管理 | 第 3 周 | 新建/列表/详情页、本地存储、同步骨架 |
| 4. 行程编辑 | 第 4-5 周 | 时间轴、活动增删改、拖拽、待定池 |
| 5. 地图集成 | 第 6 周 | 高德 POI 搜索、路线绘制、一键导航跳转 |
| 6. 预算与记账 | 第 7 周 | 预算模型、记一笔、超支预警 |
| 7. 导出功能 | 第 8-9 周 | 三种 PDF/PNG 模板、分享面板 |
| 8. 打磨与发布 | 第 10 周 | 性能优化、真机测试、内测发布 |

### 10.3 考核节点（结合作者学习目标）

| 时间点 | 可展示内容 |
|---|---|
| 2026.05 秋招启动 | SRS 文档 + UI 原型 + 部分 MVP 代码 |
| 2026.08 秋招面试 | MVP v1.0 已发布，真实在自己和朋友中使用 |
| 2026.12 考研初试 | v1.5 完成，有完整用户反馈循环 |
| 2027.03 考研复试 | v2.0 部分完成，AI 引言可演示 |

---

## 11. 风险与假设

### 11.1 项目风险

| 风险 | 等级 | 缓解措施 |
|---|---|---|
| 考研+开发双线作战时间不足 | 高 | 严格控制 MVP 范围，v1.0 只做 P0 功能 |
| 对 Flutter 不熟悉，学习曲线陡 | 中 | 前期做 1-2 个小 demo，先跑通技术栈 |
| 高德 API 配额或资质限制 | 中 | 提前申请开发者账号，了解配额；备选 Mapbox 境外方案 |
| LLM API 成本失控（v2） | 中 | 单用户额度上限、缓存生成结果、降级到预设模板 |
| 同步冲突导致数据丢失 | 高 | 严格测试、保留版本历史、关键操作二次确认 |
| iOS 审核政策限制（v2 上架） | 中 | 作者自测阶段不上架，仅使用 TestFlight；正式上架前补齐政策合规 |
| 实际使用发现产品假设错误 | 中 | 每次旅行后做 10 分钟回顾、记录使用中的不适 |

### 11.2 关键假设

- **假设 1**：作者未来一年内有至少 3 次出行，能作为真实用户持续测试
- **假设 2**：朋友愿意作为协助测试者使用产品
- **假设 3**：高德 API 的个人开发者免费额度满足早期需求（通常 100 万次/日）
- **假设 4**：LLM API 价格维持当前水平（Qwen 等约 1-10 元/百万 token）

---

## 附录

### 附录 A：术语表

| 术语 | 解释 |
|---|---|
| OTA | Online Travel Agency，在线旅行社，如携程、飞猪 |
| POI | Point of Interest，兴趣点，地图上可搜索的地点 |
| UGC | User Generated Content，用户生成内容 |
| MVP | Minimum Viable Product，最小可行产品 |
| AA 制 | 共同支出按人均摊 |
| Local-First | 本地优先架构，数据先写本地再同步云端 |
| Last-Write-Wins | 最后写入者优先，一种同步冲突策略 |

### 附录 B：相关文档

- UI 设计原型：见项目 Figma / 截图目录
- 数据模型 ER 图：`/docs/erd.png`
- 接口 OpenAPI 规范：`/docs/openapi.yaml`（后端启动后自动生成）
- 竞品分析原始数据：`/docs/competitor-research.md`

### 附录 C：作者信息

| 项目 | 内容 |
|---|---|
| 作者 | Dreamaker (刘忠鑫) |
| 学校 | 东北大学 · 软件工程（985+211） |
| 届别 | 2026 届本科毕业 |
| 职业方向 | 软件开发 / AI 算法（NLP 方向） |
| 深造目标 | 中国科学技术大学计算机科学 · 2027 年入学 |
| 项目动机 | 个人真实需求 + 秋招与考研复试代表作 |

### 附录 D：版本历史

| 版本 | 日期 | 修订内容 | 修订人 |
|---|---|---|---|
| v0.1 | 2026.04.21 | 初版文档，完成需求、设计、技术选型 | Dreamaker |

---

**文档结束**

如有疑问或建议，欢迎通过 issue 反馈。本项目为个人学习与自用工具，开源协议待定。
